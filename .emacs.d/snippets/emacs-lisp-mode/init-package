# key: init
# name: init-package
# --
;;; `(file-name-nondirectory (buffer-file-name))` --- ${1:`(cadr (split-string (file-name-base) "-"))`} -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:
;;;; Dependencies and Setup
(use-package ${2:`(cdr (split-string (file-name-base) "-"))`}
  :straight t)

$0

(provide '`(file-name-base)`)
;;; `(file-name-nondirectory (buffer-file-name))` ends here
